# Environment Variables - PRODUCTION TEMPLATE

# === MONGODB CONFIGURATION ===
# For Production: Use MongoDB Atlas or other cloud MongoDB service
# Get your connection string from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/career-nest?retryWrites=true&w=majority

# For Local Development (keep for reference):
# MONGODB_URI=mongodb://localhost:27017/career-nest

# === JWT SECRET ===
# CRITICAL: Generate a new random secret for production!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_IN_PRODUCTION

# === SERVER CONFIGURATION ===
PORT=8000
NODE_ENV=production

# === CORS CONFIGURATION ===
# Update with your actual production domain
FRONTEND_URL=https://your-production-domain.com

# === EMAIL CONFIGURATION (Optional) ===
# Set these to enable emailing credentials on approval
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# SMTP_FROM="Career Nest <no-reply@careernest.com>"

# === API KEYS (If needed) ===
# Add any third-party API keys here
# SENDGRID_API_KEY=
# STRIPE_SECRET_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
